import React, { Component } from 'react'
import { Text, View } from 'react-native'

export class UserOnly extends Component {
  render() {
    return (
      <View>
        <Text> Only User </Text>
      </View>
    )
  }
}

export default UserOnly
